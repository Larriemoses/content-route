# Factuality reviewer prompt v1

Review each claim only against the supplied evidence package. Mark unsupported, stale, ambiguous, or overextended claims as failed. Do not use outside knowledge to fill gaps. Return structured JSON with claim-level reasons and source references.
